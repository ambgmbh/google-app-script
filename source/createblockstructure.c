// REM Samplecode
typedef struct {
  // Pointer to data (can be an array or custom data structure)
  void* data;
  // Size of the data in bytes
  size_t size;
  // Additional information (optional)
  // ...
} Block;
